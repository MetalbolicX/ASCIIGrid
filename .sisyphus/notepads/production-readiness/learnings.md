+ Added ANSI sanitization at the CLI title, JSON string render, and rich-mode string cell call sites.
+ ReScript files in this workspace do not have an LSP configured, so build/test output is the practical verification path for .res changes.
